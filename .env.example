# Copy to `.env` for local development:  cp .env.example .env
#
# Points the CLI at the local manual store so `poetry run pensieve ...` works
# directly inside this repo. Leave unset in real use — Pensieve defaults to
# ~/.pensieve.
PENSIEVE_HOME=.local/manual
